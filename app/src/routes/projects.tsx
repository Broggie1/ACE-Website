import { createFileRoute } from "@tanstack/react-router";
import { PageIntro, PageShell } from "@/components/ace-site";
import { pageHead } from "@/lib/seo";
export const Route = createFileRoute("/projects")({ head: () => pageHead("Projects | Ashford Community Energy", "See how Ashford Community Energy will distinguish opportunities that are exploring, assessing, developing, delivering or operational.", "/projects"), component: ProjectsPage });
const stages = [["Exploring", "An opportunity has been identified. We are gathering enough information to decide whether assessment is worthwhile."],["Assessing", "The site, energy opportunity, ownership, constraints and initial economics are being tested."],["Developing", "A viable opportunity is moving through detailed design, permissions, commercial structure and delivery planning."],["Delivering", "Contracts, installation and commissioning are under way."],["Operational", "The project is operating and its performance and community outcomes can be reported."]] as const;
function ProjectsPage() { return <PageShell><PageIntro eyebrow="Projects" title="A clear status for every opportunity"><p>ACE will use a simple project lifecycle so residents, partners and site owners can understand what has been verified and what still needs work.</p></PageIntro><section className="ace-stage-list">{stages.map(([stage, body], index) => <article key={stage}><div className="ace-stage-list__number">0{index + 1}</div><div><h2>{stage}</h2><p>{body}</p></div></article>)}</section><section className="ace-project-note"><h2>Project information will be evidence led</h2><p>We will not present an opportunity as a live project until the underlying status and evidence are ready to share. This page is designed to grow into the public project register as ACE's pipeline develops.</p><a href="/contact#contact-form" className="ace-project-suggest">Suggest a site</a></section></PageShell>; }
