# ACE Vercel migration

This GitHub copy is intended for Vercel deployment. The original Higgsfield-hosted ACE site remains unchanged.

The website pages and assets are portable. The existing contact submission backend on the Higgsfield version uses Cloudflare D1 and therefore requires a separate Vercel-compatible storage/email integration before public launch.
